{
  "name": "sgac-college-website",
  "version": "1.0.0",
  "description": "Sethupathy Government Arts College Website",
  "private": true,
  "scripts": {
    "dev": "npx serve -l 3000",
    "build": "rm -rf dist && mkdir -p dist && cp index.html History.html Vision.html Administration.html Honours.html Principal-Desk.html Library.html developer.html admin-login.html admin.html CheckList.html dist/ && cp -r css js img Activities Courses Documents dist/",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
